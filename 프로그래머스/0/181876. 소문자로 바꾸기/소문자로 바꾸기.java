class Solution {
    public String solution(String myString) {
        String answer = "";
        // 인덱스의 문자열을 소문자로 변환
        answer = myString.toLowerCase();
        
        return answer;
    }
}